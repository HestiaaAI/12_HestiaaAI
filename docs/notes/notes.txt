Hestia AI — Team 12 notes


Week of 2026-09-16
- Created the assignment repository 12_HestiaAI and added 27guptamohit as a collaborator
- Kept the existing Django apps: households, intake, inventory, workflows
- Restructured to the assignment layout: README.md, docs/wireframes, docs/branching_strategy, docs/notes
- Split settings into base.py, development.py and production.py; DEBUG is True only in development
- Moved SECRET_KEY and a dummy OPENAI_API_KEY into .env, and added .env.example
- Copy .env.example to .env before running. Never commit .env


Section 2 — four view implementations (September 20, 2026)

Demo (populated database): http://127.0.0.1:8001/tasks/cbv-generic/

All four routes read Task rows, search titles through the GET parameter q, and
render the same tasks/task_list.html. Only the implementation changes, so identical
output is expected. Creating and assigning work comes later: Task is the reusable
definition, dates live on TaskOccurrence and responses on Assignment.

  A01  task_manual_view  /tasks/manual/       Manual FBV: loader.get_template, then HttpResponse    http://127.0.0.1:8000/tasks/manual/
  A02  task_render_view  /tasks/render/       FBV using the render() shortcut                       http://127.0.0.1:8000/tasks/render/
  A03  TaskBaseView      /tasks/cbv-base/     CBV inheriting View, explicit get()                   http://127.0.0.1:8000/tasks/cbv-base/
  A04  TaskListView      /tasks/cbv-generic/  CBV inheriting ListView, search in get_queryset()     http://127.0.0.1:8000/tasks/cbv-generic/

URL names are workflows:task-manual, task-render, task-cbv-base, task-cbv-generic.


What I found useful, and when

HttpResponse vs render
Writing task_manual_view first was worth doing once. Loading the template,
rendering it to a string and wrapping that in HttpResponse shows the three steps,
and made clear that render() returns the same HttpResponse rather than a different
kind of thing. After that I used render() everywhere here, since every response is
a full HTML page and the manual version only adds lines.

HttpResponse earns its place when there is no template. The receipt upload flow
will need JSON for fetch calls, a status code on a rejected duplicate, and a file
download. Those are HttpResponse or JsonResponse, not render.

Base CBV vs Generic CBV
TaskBaseView made the GET path explicit: I wrote get() myself, queried Task and
called render. More code than the generic version for the same page, but nothing
hidden, which helped while I was still confirming the URL names resolved to the
classes I expected.

TaskListView removed most of that. Declaring model, template_name and
context_object_name covered the list flow, and only get_queryset() needed
overriding for the search. The rule I settled on: when a view is exactly "query a
model, filter, render a list", ListView already does it; when it is not, the base
View is easier than fighting the generic one.

The trade-off showed up in something small. The view_style label was one dict key
in the base view's get(), but needed extra_context on ListView. Accepting or
declining an assignment is a POST that changes state with no matching generic
view, so that will be a base View or a form view.


Evidence — docs/screenshots/section-2/

01-04  the four routes, 1280x720 viewport, method label visible in each
05     lower portion of the generic page: type, location, recurrence, added date
06     search q="  GROCERIES  " returning the one matching task
07     search q=XYZNOTAREALTASK123 showing the no-match state
08     empty database on port 8002, no q parameter and zero Task rows
09     375x812 viewport: links wrap, cards stack, no horizontal overflow
10     320x812 viewport: Tab from search reaches the button, 3px focus outline

Unaltered JPEG viewport captures, browser chrome excluded, showing three fictional
tasks from seed_template_demo rather than live records. Manual checks: all four
links reached the intended methods, search matched trimmed uppercase input, Enter
submits, Show all tasks restores the list. No console warnings or errors.


Reproducing the demo

From the project root, using the .venv environment described in README.md. Creates
a temporary SQLite database and never touches the project's db.sqlite3. Ctrl-C to
stop. For the empty-database capture, run again in a second terminal with
HESTIA_DEMO_MODE=empty, which serves on port 8002.

```bash
HESTIA_DEMO_MODE=populated .venv/bin/python - <<'PYDEMO'
import os
import tempfile
from pathlib import Path

mode = os.environ["HESTIA_DEMO_MODE"]
assert mode in {"populated", "empty"}
os.environ["DJANGO_SETTINGS_MODULE"] = "hestia_config.settings.development"
from django.conf import settings
settings.DATABASES = {"default": {
    "ENGINE": "django.db.backends.sqlite3",
    "NAME": Path(tempfile.mkdtemp(prefix="hestia-demo-")) / "db.sqlite3",
}}
import django
django.setup()
from django.core.management import call_command
call_command("migrate", interactive=False, verbosity=0)
if mode == "populated":
    call_command("seed_template_demo")
port = "8001" if mode == "populated" else "8002"
call_command("runserver", "127.0.0.1:" + port, use_reloader=False)
PYDEMO
```


Verification

.venv/bin/python -m pip check
.venv/bin/python manage.py test --verbosity 1
.venv/bin/python manage.py check --settings=hestia_config.settings.development
.venv/bin/python manage.py check --settings=hestia_config.settings.production
.venv/bin/python manage.py makemigrations --check --dry-run

Six tests pass, no migration drift. AssignmentRoutingTests checks the four URL
names resolve to the intended distinct functions and classes. TaskTemplateTests
covers populated fields, escaping, the search, no-match and empty states, the
method labels, and omission of the teaching controls when the template has no
view_style. The label test failed for all four before implementation.


Limits

These routes are anonymous and query every Task row, so use fictional data only.
Phase 2 adds authentication and household isolation. The final product reuses the
patterns and templates, with ordinary navigation replacing the four demo links.

Week of 2026-09-21 — submission review of dev-cn32-v2
- Reviewed the branch against Sections 1, 2, and 3 of the assignment.
- Section 3 uses base.html inheritance, reusable content/search/results blocks,
  a Task loop, conditional fields, and meaningful {% empty %} output. All four
  view styles share tasks/task_list.html with the context name tasks.
- Six tests passed with development settings and again with production settings.
  No migration drift or dependency conflicts were detected. Static file discovery
  found the shared CSS. .env is ignored and has no path history in this branch.
- Fixed the README local production-settings preview: runserver with DEBUG=False
  did not serve CSS (404). The documented localhost-only --insecure preview
  returns 200 for all four pages, the empty search, and CSS. Public deployment
  requires proper static serving; --insecure is only for the local demonstration.
- Added docs/wireframes/v1/README.md so the required wireframes directory is
  included in Git once committed. Added Windows setup and rubric/evidence links
  to README.md. Existing section-2 screenshots include normal and empty states.
- Submission still needs the new README, wireframe, and this note committed and
  merged through the team workflow. Older untracked handoff/screenshots are not
  necessary for the existing submission evidence.
- Public repository check: unauthenticated GitHub API lookup for the configured
  HestiaaAI/12_HestiaaAI remote returned 404. This does not prove the repo is
  missing: it may be private. Confirm public access before submitting the link.
  Instructor collaborator access could not be independently verified.
